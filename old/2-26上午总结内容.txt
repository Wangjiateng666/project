1、css中兼容性的解决方法，总结下来
   加前缀：不同内核的
   针对Ie：

2、改变弹性盒子的排列顺序使用的是，使用的前提是必须要给做外层的元素加上flex
    flex-direction

3、flex-wrap让里边的元素放不下的话，换行显示
    no-wrap的话：根据内容进行压缩
    wrap：换行显示

4、justify-content：
     水平对齐
     center

5、align-items：
     垂直对齐
     center

6、如果想要设置水平和垂直居中浏览器的话，从html开始，到包裹的最外层的元素，都必须加上宽高：100%

分界线往上都是给外边嵌套的盒子设置的
---------------------------------------------------------------------
分界线往下都是给里边被嵌套的小盒子设置的
7、order：数值（值越小，越靠前显示）

8、flex-grow：分配的是剩余空间的比例
两端固定中间自适应

9、flex-shrink：缩小的比例



