1、正常得情况下，数据我们只需要进行提交，不需要再后台进行获取，我们提交得数据是由后台人员获取完成的，我们需要做的，是把返回得数据显示再页面中

2、js文件--js文件中有一个对象
     多个对象得话，一般我们使用得是数组来表示得
     正常情况下，从后台提取得数据是不需要进行截取得，所以这种情况下我们使用的是json文件来进行获取得

3、重要通知：每次必须打开live server
     重要通知：每次必须打开live server
     重要通知：每次必须打开live server


4、对用户名进行验证，如果数据库中存在得话那么直接显示，否则可用
     思路：
        把所有得json文件中的name全部都获取出来然后逐一和input值进行比对，如果有一样的情况下，显示：占用，否则：可用
        json文件中得数据通过ajax进行获取
       需要再ajax中添加input得事件
       用户名一但存在就不能再进行循环比较了，因为没有意思了







